clear all;
close all;
clc;

mkdir('results');

addpath data;
addpath data/calibration;
addpath data/raw_data;
addpath functions;
addpath utils;
addpath plotting;
addpath reporting;
addpath results;

rng(1000);